1. Maintain doctor/nurse arrayList according to the patients' addition/removal.

3. To print Patient Medical Record(requires all classes to interact with each other).

5. Implement exception handling(validation).
6. Create a static member function to allot the dummy doctors and nurses to the patients in the database constructor.